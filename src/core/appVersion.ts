import packageJson from "../../package.json";

export const APP_VERSION = packageJson.version;
export const SCROLLINK_VERSION_LABEL = `S/COM_OS v${APP_VERSION}`;
export const SCROLLINK_BUILD_LABEL = `S/COM_OS BUILD ${APP_VERSION}`;
