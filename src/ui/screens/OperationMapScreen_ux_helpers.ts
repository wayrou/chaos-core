
// ============================================================================
// NEW UX FUNCTIONS - Node Interaction
// ============================================================================

/* function updateDebugBanner(): void {
    if (!FEATURE_NEW_MAP_UX) return;
    const selectedSpan = document.getElementById('debug-selected-node');
    const hoveredSpan = document.getElementById('debug-hovered-node');
    if (selectedSpan) selectedSpan.textContent = selectedNodeId || 'null';
    if (hoveredSpan) hoveredSpan.textContent = hoveredNodeId || 'null';
} */
